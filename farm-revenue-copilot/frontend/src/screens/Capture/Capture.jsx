import React from 'react';

/**
 * Capture screen — photo/sensor data capture entry point.
 * Farmer uploads crop image or logs field observation.
 */
export default function Capture() {
  return (
    <main>
      <h1>Capture</h1>
      {/* TODO: ImageUpload, ObservationForm, SubmitButton */}
    </main>
  );
}
