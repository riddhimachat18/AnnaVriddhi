import React from 'react';

/**
 * SchemeMatcher screen — shows government/bank schemes the
 * farmer is eligible for, with application links.
 */
export default function SchemeMatcher() {
  return (
    <main>
      <h1>Scheme Matcher</h1>
      {/* TODO: EligibilityFilter, SchemeCardList, ApplyButton */}
    </main>
  );
}
